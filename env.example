# =============================================================================
# Greenback Finance Intelligence Platform — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your credentials:
#   cp env.example .env
#
# This file should ONLY contain secrets and credentials.
# All other configuration lives in config/config.yaml.
# =============================================================================

# QGenie API key (REQUIRED)
QGENIE_API_KEY=your-api-key-here

# PostgreSQL credentials (REQUIRED for production)
POSTGRES_ADMIN_USER=postgres
POSTGRES_ADMIN_PWD=postgres
POSTGRES_USER=your_db_user
POSTGRES_PWD=your_db_password
